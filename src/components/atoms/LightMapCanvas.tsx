import React from 'react'

export default function LightMapCanvas() {
  // Canvas, SVG, or CSS
  // show location, color, shape of stars based on fan data
  // when clicked, show detail view of the star as pop-up
  return (
    <div>LightMapCanvas</div>
  )
}
